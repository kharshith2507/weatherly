# OpenWeatherMap API Key
# Get your free API key at: https://openweathermap.org/api
# Server-only — do NOT prefix with NEXT_PUBLIC_, this must never reach the browser.
OPENWEATHER_API_KEY=your_api_key_here
